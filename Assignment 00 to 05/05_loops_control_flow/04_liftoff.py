def main():
    curr_value = input("Enter a number: ")
    
    curr_value = int(curr_value)  
    
           
    while curr_value < 100:
        curr_value = curr_value * 2    
        print(curr_value)
    
main()